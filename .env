# LiveKit Dev Server Configuration
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret
LIVEKIT_WS_URL=ws://127.0.0.1:7880

# Server Configuration
PORT=3000

# Note: These are the default values for the LiveKit dev server
# Run the dev server with: livekit-server --dev
# For network access: livekit-server --dev --bind 0.0.0.0